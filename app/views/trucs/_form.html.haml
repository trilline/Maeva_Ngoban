= form_with(model: truc) do |form|
  - if truc.errors.any?
    %div{:style => "color: red"}
      %h2
        = pluralize(truc.errors.count, "error")
        prohibited this truc from being saved:
      %ul
        - truc.errors.each do |error|
          %li= error.full_message
  %div
    = form.label :name, style: "display: block"
    = form.text_field :name
  %div
    = form.submit
